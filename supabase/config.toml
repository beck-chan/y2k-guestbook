# Edge Functions for admin notification emails (Resend).
# Remote project ref is stored in supabase/.temp (gitignored).

[functions.notify-admins]
verify_jwt = false
static_files = ["./functions/_shared/templates/*.md"]

[functions.on-user-created]
verify_jwt = false
static_files = ["./functions/_shared/templates/*.md"]
